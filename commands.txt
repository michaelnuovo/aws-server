// Server

$ ssh -i "~/.ssh/aws-test-server.pem" ec2-user@ec2-35-166-124-211.us-west-2.compute.amazonaws.com # ssh into server
$ cd server # change to server directory

$ ps -ef | grep node # list all node proccesses
$ pkill -f node      # kill all node proccesses

ctrl+z  # list running processes
$ bg %1 # run server process 1 in background
$ exit  # log out

// GIT

$ cd ~/Dropbox/gitignore.txt

$ git init 
$ git add .
$ git commit -m "message"
# git push origin master